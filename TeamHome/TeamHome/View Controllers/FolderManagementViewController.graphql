mutation MoveToFolder($id: ID!, $title: String){
    updateFolderTitle(
                  folderId: $id
                  title: $title
                  
                  }) {
        id
        title
    }
}

