query FetchAllUsers {
    users {
        id
        name
        role
        email
        profilePic
        phone
    }
}

mutation AddUserToTeam ($teamId: ID!, $userId: ID!) {
    addUserToTeam(teamId: $teamId, userId: $userId) {
        id
    }
}
