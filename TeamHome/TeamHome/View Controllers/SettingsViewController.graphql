mutation UpdateUser($name: String, $email: String, $phoneNumber: String, $avatar: String) {
    updateUserContactInfo(
               name
               email: $email
               phone: $phoneNumber
               profilePic: $avatar
                )
#toggles {
#receiveEmails: $receiveEmails
#receiveTexts: $receiveTexts
#}
            {
        id
    }
}
#mutation updateTeam(
                    $id: ID!
                    $name: String
                    $users: [TeamUserInput]
                    $premium: Boolean
                    ) {
#   updateTeam(
               input: { id: $id, name: $name, users: $users, premium: $premium }
               ) {
        _id
    }
}
